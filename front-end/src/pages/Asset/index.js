import RequiredAdmin from '../../context/RequiredAdmin/requiredAdmin';

function Asset() {
    return (
        <RequiredAdmin>
            <h1>Asset </h1>
        </RequiredAdmin>
    );
}

export default Asset;
