import RequiredAdmin from '../../context/RequiredAdmin/requiredAdmin';

function Request() {
    return (
        <RequiredAdmin>
            <h1>Request </h1>
        </RequiredAdmin>
    );
}

export default Request;
